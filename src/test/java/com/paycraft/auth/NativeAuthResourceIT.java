package com.paycraft.auth;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeAuthResourceIT extends AuthResourceTest {

    // Execute the same tests but in native mode.
}